import React, { Component } from 'react';

import './Peer.css';

class Peers extends Component {
  render() {
    return (
      <div className="Peers">Peers</div>
    );
  }
}

export default Peers;
